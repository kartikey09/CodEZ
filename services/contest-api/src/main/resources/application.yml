server:
  port: 8080

spring:
  application:
    name: contest-api

# Only health + info exposed for now; we add db/redis indicators on later days.
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
