# Only needed if your service ports differ from the defaults.
# VITE_AUTH_ORIGIN=http://localhost:8081   # auth-service
# VITE_API_ORIGIN=http://localhost:8080    # contest-api
# VITE_WS_ORIGIN=ws://localhost:8083       # realtime (WebSocket)
# VITE_CONTEST_ID=1                        # Leaderboard defaults to the running contest; set this to look at a past one
